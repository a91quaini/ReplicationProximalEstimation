% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/monte_carlo.R
\name{sim_analysis_est}
\alias{sim_analysis_est}
\title{Estimation analysis under vanishing measurement error}
\usage{
sim_analysis_est(
  n_grid,
  cases = c("regular", "singular", "nearly_singular"),
  regimes = c("dense", "sparse"),
  R = 5000L,
  gamma = 3/4,
  sigma_u = sqrt(2),
  rho = 1/3,
  nu_fun = function(n) n^(-3/8),
  seed = NULL
)
}
\arguments{
\item{n_grid}{Integer vector of sample sizes.}

\item{cases}{Character vector in c("regular","singular","nearly_singular").}

\item{regimes}{Character vector in c("dense","sparse").}

\item{R}{Number of Monte Carlo replications per (case, regime, n).}

\item{gamma}{Exponent for \eqn{\lambda_n = n^{-\gamma}} (default 3/4).}

\item{sigma_u}{Error SD.}

\item{rho}{Correlation parameter for Delta construction.}

\item{nu_fun}{Function n -> nu_n for MRL and for Qbar_n (default n^{-3/8}).}

\item{seed}{Optional integer seed (set once at function entry).}
}
\value{
Nested list indexed by case, then regime, then n (as character).
}
\description{
Simulates data under the measurement-error design and computes, for each replication,
the following estimators:
(i) ridgeless (RL),
(ii) modified ridgeless (MRL),
(iii) adaptive ridgeless proximal estimator (RL-AL; ridgeless initial, metric Q_n),
(iv) adaptive modified-ridgeless proximal estimator (MRL-AL; modified ridgeless initial,
metric Qbar_n).
}
