% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimators.R
\name{est_prox_mrl_adalasso}
\alias{est_prox_mrl_adalasso}
\title{Proximal adaptive lasso based on modified ridgeless, solved via glmnet}
\usage{
est_prox_mrl_adalasso(b = NULL, X, y = NULL, nu, lambda)
}
\arguments{
\item{b}{Initial estimator (numeric vector of length p). If NULL, it is computed as
\eqn{\check\beta_n^{ls}} from (X,y,nu).}

\item{X}{Regressor matrix (n x p) used to construct \eqn{Q_n=X'X/n}.}

\item{y}{Outcome vector (length n). Required only if b is NULL.}

\item{nu}{Eigenvalue threshold \eqn{\nu_n} used to form \eqn{\check Q_n}.}

\item{lambda}{Nonnegative tuning parameter \eqn{\lambda_n}.}
}
\value{
Numeric vector of coefficients (length p).
}
\description{
Implements the adaptive-lasso proximal step with metric \eqn{\overline{\check Q}_n}.
}
